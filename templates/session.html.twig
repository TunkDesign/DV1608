{% extends "base.html.twig" %}

{% block title %}Hem{% endblock %}

{% block body %}
<section class="about">
  <a href="{{ path('session_delete') }}">Radera Session Data</a>
  <div class="container">
    <h2>Session Data</h2>
    <ul>
      {% for ln in session %}
          <li>{{ ln }}</li>
      {% endfor %}
    </ul>

    
    {{ include('pig/flash.html.twig') }}
  </div>
</section>
{% endblock %}